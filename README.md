# Network Commands 
## Commands Included

- `nslookup` – Get the IP address of a domain
- `ping` – Test network connectivity
- `htop` – Monitor CPU and memory usage
- `nc (netcat)` – Check if a specific port is open
- `telnet` – Test connectivity to a port
- `nmap` – Scan a specific port on a domain
